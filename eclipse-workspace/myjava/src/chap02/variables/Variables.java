package chap02.variables;

public class Variables {
	public static void main(String[] args) {
		System.out.println("변수에 대해서 배워보자");
		System.out.println("sysout은 sout으로도 쓸 수 있다.");
//	System.out.println("다음시간에 이어서 합시다.");

		// 변수는 하나의 값을 저장할 수 있는 공간의 이름

		// 변수 선언
		// 타입과 변수명
		int a;
		double value;

		int x, y, z;

		// 영문대소문자, $, _, 숫자의 조합
		// 숫자가 앞에 오면 안됨
		int $;
		int $value;
		int _age;
		int age_of_person;
		int city6;
		//	int 3city; 숫자가 앞에 오면 안됨
		
		// 변수명 작성 관습 lowerCamelCase
		// 소문자로 작성, 여러 단어가 결합된 경우 앞글자는 대문자
		int ageOfPerson;
		int lowerCamelCase;
		
		// 예약어(keyword)는 변수명으로 작성 불가능
		// int int;
		// int class;
		// int public;
		
		
	}
}
